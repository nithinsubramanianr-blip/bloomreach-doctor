/**
 * Brand identity — single source of truth for the demo's display names.
 *
 * These are placeholders so the storefront and dashboard carry no real-world
 * brand IP. Change them here and the whole app updates. `store` is the
 * shopper-facing storefront name; `dashboard` is the internal tool name.
 */
export const BRAND = {
  store: "Bounteous",
  dashboard: "BloomBridge",
} as const;

export default BRAND;
